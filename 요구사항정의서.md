** API 기본 정보 **
엔드포인트: GET /cosmetics/{cosmetic_id}
설명: 특정 화장품의 상세 정보를 조회
인증: 필요 (로그인한 사용자만 접근 가능)


## 실제 흐름 예시
```
[프론트엔드] 
  ↓ GET /cosmetics/1 요청
  
[routers/cosmetic.py] ← API 라우터
  ↓ "1번 화장품 정보 주세요"
  
[models/cosmetic.py] ← DB 모델 사용
  ↓ Cosmetic 테이블에서 id=1인 데이터 조회
  
[MySQL Database]
  ↓ 데이터 반환
  
[routers/cosmetic.py]
  ↓ 좋아요 수 계산, 이미지 URL 조합 등 추가 처리
  
[프론트엔드]
  ← JSON 응답 받음
```

---

## 📋 개발 계획 (GET /cosmetics/{cosmetic_id})

### 🎯 구현 목표
특정 화장품의 상세 정보를 조회하는 API 엔드포인트 개발

### 📂 작업 파일 목록

#### 1️⃣ Schema 추가 (`backend/app/schemas/cosmetic.py`)
**작업 내용:**
- `CosmeticDetailResponse` 클래스 추가
- 응답 필드 정의:
  - `cosmetic_id`, `brand`, `name`, `category`, `price`
  - `short_description`, `description`, `buy_url`
  - `skin_type`, `skin_disease`, `main_effect`, `care_symptom`
  - `key_ingredient`, `ingredients`
  - `file_path` (이미지 경로)
  - `like_count` (좋아요 개수)
  - `is_liked` (사용자 좋아요 여부)

**참고:**
- `Decimal` 타입 사용 (가격)
- `Optional` 타입 활용 (nullable 필드)
- `orm_mode = True` 설정

#### 2️⃣ Repository 메서드 추가 (`backend/app/repository/cosmetic.py`)
**작업 내용:**
- `get_detail(db, cosmetic_id, member_id)` static 메서드 추가
- 구현 내용:
  - `Cosmetic` 테이블에서 기본 정보 조회
  - `File` 테이블 LEFT JOIN으로 대표 이미지 조회
  - `Like` 테이블로 좋아요 개수 계산
  - `member_id`가 있으면 해당 사용자의 좋아요 여부 확인
  - 모든 정보를 딕셔너리로 반환

**쿼리 패턴:**
- 기존 `search()` 메서드의 서브쿼리 패턴 재사용
- `scalar_subquery()` 활용

#### 3️⃣ Service 메서드 추가 (`backend/app/services/cosmetic.py`)
**작업 내용:**
- `get_cosmetic_detail(db, cosmetic_id, member_id)` static 메서드 추가
- 로직:
  1. Repository에서 데이터 조회
  2. 데이터가 없으면 `ApiException(404, "화장품을 찾을 수 없습니다")` 발생
  3. `CosmeticDetailResponse` 스키마로 변환하여 반환

**예외 처리:**
- 404: 화장품 없음

#### 4️⃣ Router 엔드포인트 추가 (`backend/app/router/cosmetic.py`)
**작업 내용:**
- `GET /api/cosmetics/{cosmetic_id}` 엔드포인트 추가
- 파라미터:
  - `cosmetic_id`: Path Parameter (int)
  - `member_id`: Query Parameter (Optional[int]) - 로그인 사용자 ID
- 응답: `ApiResponse` 형식으로 감싸서 반환

**엔드포인트 형식:**
```python
@router.get("/{cosmetic_id}", response_model=ApiResponse)
def get_cosmetic_detail(
    cosmetic_id: int,
    member_id: Optional[int] = Query(None),
    db: Session = Depends(get_db)
):
    result = CosmeticService.get_cosmetic_detail(db, cosmetic_id, member_id)
    return ApiResponse(
        code=200,
        success=True,
        message="화장품 상세 조회 성공",
        data=result
    )
```

### 🔍 기존 코드 활용
- ✅ `CosmeticRepository.search()` - 서브쿼리 패턴 참고
- ✅ `LikeRepository` - 좋아요 로직 참고
- ✅ `FileRepository.get_by_entity()` - 파일 조회 패턴 참고
- ✅ `ApiResponse` - 공통 응답 형식
- ✅ `ApiException` - 예외 처리

### ⚠️ 구현 시 주의사항
1. **인증 처리**: 현재 인증 시스템이 없어 `member_id`를 쿼리 파라미터로 받음
2. **이미지 처리**: `file_path` 반환, 프론트에서 `/api/files/{file_id}` 사용
3. **필드명 매핑**:
   - 요구사항 `product_name` → 실제 DB `name`
   - 요구사항 `product_type` → 실제 DB `category`
   - 요구사항 `oliveyoung_url` → 실제 DB `buy_url`
4. **NULL 처리**: Optional 필드는 기본값 제공

### 📝 작업 순서
1. Schema 작성 (응답 구조 정의)
2. Repository 메서드 작성 (데이터 조회 쿼리)
3. Service 메서드 작성 (비즈니스 로직)
4. Router 엔드포인트 작성 (API 노출)